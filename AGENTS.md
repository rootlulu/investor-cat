# news-digest-web 项目规则

## 服务进程

- 修改完成后允许重启当前项目服务；禁止新开第二个前端服务、第二个 Vite 实例或新端口。
- 前端验证只复用当前 `127.0.0.1:5174` 服务。需要重启时，停止并重启同一进程组；若当前前端未运行，不得自行执行 `npm run dev` 或 `scripts/start-dev.sh`，先向用户说明。
- `npm run build` 仅生成静态产物，不属于启动前端服务，可以用于验证。

## 项目知识库

- 开始任务前读取 `.codex/knowledge/README.md` 及其索引的相关知识文件。
- 只记录已验证、可复用、项目特定的事实与决策；临时日志、猜测、Token、Cookie 和个人数据不得写入知识库。
- 行为、接口、验证命令或运行约束发生持久变化时，同步更新对应知识文件。

## 工作区与验证

- 保留所有现有未提交修改和运行数据；不得覆盖、还原或清理任务范围外文件。
- Python 验证使用项目 `.venv/bin/python`；前端验证使用现有 `npm run build`。
- 先运行最小相关测试，再按影响范围运行全量测试、构建与 `git diff --check`。
